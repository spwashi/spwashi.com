# GitHub Copilot instructions for spwashi.com

This adapter emphasizes one focus. AGENTS.md is the gate. Any model still follows Open first.

See `GPT.md` (Contract Exactness + computer-use verify-first) and `CLAUDE.md` (Constitutional Rigor). Do not generate React, Vue, Svelte, or Tailwind. Browser ESM uses `.js` import paths. `npm run check:local`. Commits close with `#[episode]{ ~[scene]{} ![change]{} *[verify]{} }`.
