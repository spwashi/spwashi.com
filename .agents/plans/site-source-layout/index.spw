# Review 2026-07-12 — retain as a long-horizon migration prime; do not move the authored tree without ref-safe evidence and human review.
# Plan Index — site-source-layout
#
# The desired end state is a repo where: - the authored website lives under one obvious source root: site/ - generated deploy output continues to live in dist/ - build/dev/check…

#>plan_site_source_layout_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `The desired end state is a repo where: - the authored website lives under one obvious source root: site/ - generated deploy output continues to live in dist/ - build/dev/check scripts continue to work without changing public URLs - non-site concerns such as .agents/, .spw/, .github/, and tooling no longer visually compete with route folders at the repo root - templates and partials stay inside the site source tree as non-routable authoring surfaces This is a repository-clarity change, not a framework migration. The public site should still behave like a hand-authored static site with root-relative asset paths and directory routing.`
 owner = `site-source-layout`
 kind = `plan`
 bucket = `runtime_js_validation`
 artifact_shape = `PLAN.md | wip.spw | index.spw`
}[reg=facet]

^"dimensions"{
 practice_depth = "reading | observation | tending"
 semantic_layer = "pragmatics"
 collaboration_phase = "tending"
 memory_tier = "hot"
 bucket = "runtime_js_validation"
}[reg=facet]

^"conceptual_model"{
 thesis = `The desired end state is a repo where: - the authored website lives under one obvious source root: site/ - generated…`
 concepts = #[
  `repo`,
  `source`,
  `root`,
  `site-source-layout`,
  `desired`,
 ][reg=set]
 surfaces = #[
  `scripts/`,
  `.spw/`,
  `scripts/build.mjs`,
  `scripts/dev-server.mjs`,
 ][reg=set]
 fixity_tier = `tending`
}[reg=facet]


@plans_root: ~"../../../.agents/plans/index.spw"
@plan_index_convention: ~"../../../.spw/conventions/plan-index.spw"
@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@wip: ~"./wip.spw"
@plan_semantic_html_normalization: ~"../semantic-html-normalization/index.spw"
@plan_css_maintainability_refactor: ~"../css-maintainability-refactor/index.spw"
@plan_runtime_module_fluency: ~"../runtime-module-fluency/index.spw"
@plan_spw_surface_normalization: ~"../spw-surface-normalization/index.spw"
@scripts: ~"../../../scripts/"
@spw: ~"../../../.spw/"
@build: ~"../../../scripts/build.mjs"
@dev_server: ~"../../../scripts/dev-server.mjs"
@template: ~"../../../scripts/template.mjs"
@generate_sitemap: ~"../../../scripts/generate-sitemap.mjs"
@generate_design_catalog: ~"../../../scripts/generate-design-catalog.mjs"
@site_contracts: ~"../../../scripts/site-contracts.mjs"

dispatch: .{
 plan = @plan
 wip = @wip
 plan_semantic_html_normalization = @plan_semantic_html_normalization
 plan_css_maintainability_refactor = @plan_css_maintainability_refactor
 plan_runtime_module_fluency = @plan_runtime_module_fluency
 plan_spw_surface_normalization = @plan_spw_surface_normalization
 scripts = @scripts
 spw = @spw
 build = @build
 dev_server = @dev_server
 template = @template
 generate_sitemap = @generate_sitemap
 generate_design_catalog = @generate_design_catalog
 site_contracts = @site_contracts
}[reg=facet]

^"research_bridge"{
 local_research = #[
  .{ label = `javascript_module_census` ; path = `.spw/audits/javascript-module-census-2026-07.spw` }[reg=facet],
  .{ label = `module_export_audit` ; path = `.spw/audits/module-export-standalone-2026-07.spw` }[reg=facet],
 ][reg=set]
 rule = `Repo-local audits/appendices/conventions — not external web.`
}[reg=facet]

^"connection_points"{
 related_plans = #[
  `semantic-html-normalization`,
  `css-maintainability-refactor`,
  `runtime-module-fluency`,
  `spw-surface-normalization`,
 ][reg=set]
}[reg=facet]

^"owner_claim"{
 claim_id = "plan-site_source_layout-001"
 layer = "pragmatic"
 hypothesis = `The desired end state is a repo where: - the authored website lives under one obvious source root: site/ - generated deploy output continues to live in dist/ - build/dev/check scripts continue to work without changing public URLs - non-site concerns such as .agents/, .spw/, .github/, and tooling no longer visually compete with route folders at the repo root - templates and partials stay inside the site source tree as non-routable authoring surfaces This is a repository-clarity change, not a framework migration. The public site should still behave like a hand-authored static site with root-relative asset paths and directory routing.`
 spec_ref = ~"./PLAN.md"
 impl_ref = ~"../../../scripts/"
 probe_ref = `git diff --check`
 falsification = `A patch cannot satisfy the public goal while leaving ../../../scripts/ unchanged or untested.`
 status = "active"
}[reg=facet]

scope: .{
 in = #[
  `In scope:`,
  `repo structure around a new site/ source root`,
  `moving route HTML, static assets, partials, service worker, and web manifest into site/`,
  `updating build/dev/check/catalog/sitemap/template scripts to understand site/`,
 ][reg=set]
 out = #[
  `framework adoption`,
  `bundler adoption`,
  `changing route URLs`,
 ][reg=set]
}[reg=facet]

validation: #[
 `git diff --check`,
 `node --check for each edited script module`,
 `npm run build`,
 `npm run check`,
 `targeted route checks in local dev on:`,
 `/`,
][reg=set]

editor_prompts: #[
 .{ ask = `What does site-source-layout own, and what should stay out of scope?`, see = @plan }[reg=facet],
 .{ ask = `Which file should I open first to implement the next slice of site-source-layout?`, see = @index }[reg=facet],
 .{ ask = `What command or check falsifies whether site-source-layout still holds?`, see = @plan }[reg=facet],
][reg=set]

invariants: #[
 `Owner intent: The desired end state is a repo where: - the authored website lives under one obvious source root: site/ - generated deploy output…`,
 `PLAN.md or FIX.md sequences; index.spw routes inspection and selection.`,
 `Promote durable owner_claim patterns to .spw/slices/ or .spw/conventions/.`,
][reg=set]
