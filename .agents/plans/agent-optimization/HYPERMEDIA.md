# Hypermedia geometry — September 2026

## Intent
Make the language atlas a usable instrument for Spwashi's own thinking: discover
which relations survive a transformation, follow a source into tools, and move
between frames without losing state or keyboard focus.

Operation sequence: audit, then separate align patches. Stable: operator meaning,
URLs, native controls, layer order. Tending: selectors, lifecycle, local ink.
Experimental: metamaterial as a cognitive model, not a physical claim.

## Evidence and implementation
- [x] Align public module hosts with `.spw-frame` / `.spw-chip`; remove vestigial aliases from the catalog and the modules it mounts here.
- [x] Repair navigator query preservation, hidden frames, focus, and teardown.
- [x] Resolve attention locally; preserve keyboard focus over transient hover.
- [x] Publish workbench improvement evidence and a reversible square-symmetry experiment
  on `/topics/software/spw/`, with native buttons and a readable static state.
- [ ] Remaining idle modules still query retired nouns; do not dual-list aliases back into the catalog. Next slice names those files one family at a time.

## Theory examined
Workbench `form-geometry.spw` distinguishes invertible surface edits from semantic
equivalence. `form-geometry-editor` requires source-specific receipts and a thin
client. `symmetry-explorer-instrument` proposes D4 on a square. Use these as
constraints for a site experiment, not evidence that Spw meanings form D4.

HTML supplies address and containment; CSS supplies a response law; JS supplies
state transitions; `.spw` supplies claims and probes. Metamaterial is useful for
composition-dependent response. A labeled graph with local response rules is more
precise for navigation. No new parser grammar, physics simulation, or dependencies.

## Validation
Baseline: module-selector audit, pocket visual checks, language ecology, integrity.
Behavior: real browser at pocket and desktop; keyboard, touch, reduced motion,
query links, hidden frames, remount, symmetry identity and composition order.
Gate: targeted module tests, regenerated bundles/manifests, check:local, ecology,
visual:checks and diff hygiene. Commit only coherent verified patches.
