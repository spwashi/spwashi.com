# Plan Index — JS Taxonomy Cleanup
#
# Remove the spw- prefix from the JavaScript source tree, keep the runtime naming semantically legible by folder, and consolidate obvious shared primitives into their owning layer.

#>plan_js_taxonomy_cleanup_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Remove the spw- prefix from the JavaScript source tree, keep the runtime naming semantically legible by folder, and consolidate obvious shared primitives into their owning layer.`
 owner = `js-taxonomy-cleanup`
 kind = `plan`
}[reg=facet]

@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@js: ~"../../../public/js/"
@ts: ~"../../../public/ts/"
@kernel: ~"../../../public/js/kernel/"
@interface: ~"../../../public/js/interface/"
@runtime: ~"../../../public/js/runtime/"
@semantic: ~"../../../public/js/semantic/"
@media: ~"../../../public/js/media/"
@modules: ~"../../../public/js/modules/"

dispatch: .{
 plan = @plan
 js = @js
 ts = @ts
 kernel = @kernel
 interface = @interface
 runtime = @runtime
 semantic = @semantic
 media = @media
 modules = @modules
}[reg=facet]

scope: .{
 in = #[
  `Rename prefixed source files in public/js/ and the TypeScript sources in public/ts/.`,
  `Update imports and file references in the site runtime, helper modules, and JS docs.`,
  `Keep behavior stable; this pass is about naming, ownership, and path clarity.`,
 ][reg=set]
}[reg=facet]

validation: #[
 `node --check on touched JS files`,
 `git diff --check`,
 `npm run check`,
][reg=set]

invariants: #[
 `Owner intent: Remove the spw- prefix from the JavaScript source tree, keep the runtime naming semantically legible by folder, and consolidate obvious…`,
 `PLAN.md or FIX.md remains the sequencing source; this index routes inspection.`,
 `Promote durable claims to named .spw artifacts, .spw/slices/, or .spw/conventions/.`,
][reg=set]

