# Canopy × material depth

Operation: align. Fixity: tending. Ladder: palette-depth.

Host families: `.spw-frame` and `.spw-svg-figure`, owned by
`public/css/effects/material.css`. SVG surfaces spend the same inset through
`--spw-svg-highlight-inset` and lift with `--material-shadow-lift`.
Replace the fixed white inset with material specular multiplied by local canopy
light. Resolve inputs on the host because root-derived attenuation cannot observe
descendant reports. About's frame shadow must consume the same inset.
Keep ink, layout, hue, and other host families. Do not invent a third optics
formula for diagrams.

Validation: module-selector census; about-years pocket still before/after;
check:css; check:agents; git diff --check. Baseline capture passed but showed
existing clipped text, outside this optics slice.
