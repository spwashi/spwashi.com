# Plan Index — shell-semantic-physics
#
# Turn the breadcrumb and hamburger menu into inspectable, semantically meaningful shell instruments. The breadcrumb should expose route, frame, mode, and shell state as separate…

#>plan_shell_semantic_physics_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Turn the breadcrumb and hamburger menu into inspectable, semantically meaningful shell instruments. The breadcrumb should expose route, frame, mode, and shell state as separate readable parts with reversible interactions. The menu should project its own operational physics through stable datasets, explicit custom events, and CSS that documents how pressure, phase, topology, and return paths work.`
 owner = `shell-semantic-physics`
 kind = `plan`
}[reg=facet]

@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@cognitive_navigation: ~"../../../.spw/conventions/cognitive-navigation.spw"

dispatch: .{
 plan = @plan
 cognitive_navigation = @cognitive_navigation
}[reg=facet]

scope: .{
 in = #[
  `In scope: shared shell JS, breadcrumb trace structure, nav-toggle semantics, CSS behavior documentation, and .spw convention updates for shell reversibility and cognitive navigation.`,
  `-`,
 ][reg=set]
}[reg=facet]

validation: #[
 `git diff --check`,
 `node --check public/js/spw-shell-disclosure.js`,
 `node --check public/js/spw-experiential.js`,
 `npm run build`,
][reg=set]

invariants: #[
 `Owner intent: Turn the breadcrumb and hamburger menu into inspectable, semantically meaningful shell instruments. The breadcrumb should expose route,…`,
 `PLAN.md or FIX.md remains the sequencing source; this index routes inspection.`,
 `Promote durable claims to named .spw artifacts, .spw/slices/, or .spw/conventions/.`,
][reg=set]

