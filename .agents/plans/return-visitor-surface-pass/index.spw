# Plan Index — return-visitor-surface-pass
#
# Refresh the main hub routes so they reward repeat visitors instead of re-explaining the site from zero every time. The public outcome should be more movement, more reasons to…

#>plan_return_visitor_surface_pass_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Refresh the main hub routes so they reward repeat visitors instead of re-explaining the site from zero every time. The public outcome should be more movement, more reasons to revisit, and more layout/component variety across the top-level surfaces.`
 owner = `return-visitor-surface-pass`
 kind = `plan`
 bucket = `active_backlog`
 artifact_shape = `PLAN.md | index.spw`
}[reg=facet]

^"dimensions"{
 practice_depth = "reading | observation | tending"
 semantic_layer = "pragmatics"
 collaboration_phase = "tending"
 memory_tier = "hot"
 bucket = "active_backlog"
}[reg=facet]

@plans_root: ~"../../../.agents/plans/index.spw"
@plan_index_convention: ~"../../../.spw/conventions/plan-index.spw"
@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@topics_index_html: ~"../../../topics/index.html"
@play_index_html: ~"../../../play/index.html"
@about_index_html: ~"../../../about/index.html"

dispatch: .{
 plan = @plan
 topics_index_html = @topics_index_html
 play_index_html = @play_index_html
 about_index_html = @about_index_html
}[reg=facet]

^"owner_claim"{
 claim_id = "plan-return_visitor_surface_pass-001"
 layer = "pragmatic"
 hypothesis = `Refresh the main hub routes so they reward repeat visitors instead of re-explaining the site from zero every time. The public outcome should be more movement, more reasons to revisit, and more layout/component variety across the top-level surfaces.`
 spec_ref = ~"./PLAN.md"
 impl_ref = ~"../../../topics/index.html"
 probe_ref = `git diff --check`
 falsification = `A patch cannot satisfy the public goal while leaving ../../../topics/index.html unchanged or untested.`
 status = "active"
}[reg=facet]

scope: .{
 in = #[
  `In scope: home, topics, services, play, and about hub copy/layout updates; one shared reusable return-visitor component family in shared CSS; selective social-image diversification where a hub still points at a generic card.`,
  `-`,
 ][reg=set]
}[reg=facet]

validation: #[
 `git diff --check`,
 `targeted rg checks for new ids/classes and updated image refs`,
 `sanity-check surrounding markup for balanced tags and route flow`,
][reg=set]

editor_prompts: #[
 .{ ask = `What does return-visitor-surface-pass own, and what should stay out of scope?`, see = @plan }[reg=facet],
 .{ ask = `Which file should I open first to implement the next slice of return-visitor-surface-pass?`, see = @index }[reg=facet],
 .{ ask = `What command or check falsifies whether return-visitor-surface-pass still holds?`, see = @plan }[reg=facet],
][reg=set]

invariants: #[
 `Owner intent: Refresh the main hub routes so they reward repeat visitors instead of re-explaining the site from zero every time. The public outcome…`,
 `PLAN.md or FIX.md sequences; index.spw routes inspection and selection.`,
 `Promote durable owner_claim patterns to .spw/slices/ or .spw/conventions/.`,
][reg=set]

