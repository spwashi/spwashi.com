# Plan Index — CSS-Sensitive Attribute Writes
#
# Keep interaction state visually responsive without letting runtime discovery casually rewrite author-owned semantic attributes that CSS uses for route, context, wonder, and…

#>plan_css_sensitive_attribute_writes_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Keep interaction state visually responsive without letting runtime discovery casually rewrite author-owned semantic attributes that CSS uses for route, context, wonder, and operator styling.`
 owner = `css-sensitive-attribute-writes`
 kind = `plan`
 bucket = `active_backlog`
 artifact_shape = `PLAN.md | index.spw`
}[reg=facet]

^"dimensions"{
 practice_depth = "reading | tending"
 semantic_layer = "pragmatics"
 collaboration_phase = "tending"
 memory_tier = "hot"
 bucket = "active_backlog"
}[reg=facet]

@plans_root: ~"../../../.agents/plans/index.spw"
@plan_index_convention: ~"../../../.spw/conventions/plan-index.spw"
@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"

dispatch: .{
 plan = @plan
}[reg=facet]

^"owner_claim"{
 claim_id = "plan-css_sensitive_attribute_writes-001"
 layer = "pragmatic"
 hypothesis = `Keep interaction state visually responsive without letting runtime discovery casually rewrite author-owned semantic attributes that CSS uses for route, context, wonder, and operator styling.`
 spec_ref = ~"./PLAN.md"
 impl_ref = ~"./PLAN.md"
 probe_ref = `git diff --check`
 falsification = `A patch cannot satisfy the public goal while leaving ./PLAN.md unchanged or untested.`
 status = "active"
}[reg=facet]

scope: .{
 in = #[
  `public/js/brace-gestures.js`,
  `Treat gesture/charge/pin attributes as transient visual state.`,
  `Treat data-spw-resolved-* attributes as optional inspection hints.`,
  `Avoid rewriting data-spw-context, data-spw-wonder, or data-spw-operator except for explicit user actions or future explicit opt-in.`,
 ][reg=set]
}[reg=facet]

validation: #[
 `git diff --check`,
 `node --check public/js/brace-gestures.js`,
 `npm run check`,
 `npm run build`,
][reg=set]

editor_prompts: #[
 .{ ask = `What does css-sensitive-attribute-writes own, and what should stay out of scope?`, see = @plan }[reg=facet],
 .{ ask = `Which file should I open first to implement the next slice of css-sensitive-attribute-writes?`, see = @index }[reg=facet],
 .{ ask = `What command or check falsifies whether css-sensitive-attribute-writes still holds?`, see = @plan }[reg=facet],
][reg=set]

invariants: #[
 `Owner intent: Keep interaction state visually responsive without letting runtime discovery casually rewrite author-owned semantic attributes that CSS…`,
 `PLAN.md or FIX.md sequences; index.spw routes inspection and selection.`,
 `Promote durable owner_claim patterns to .spw/slices/ or .spw/conventions/.`,
][reg=set]

