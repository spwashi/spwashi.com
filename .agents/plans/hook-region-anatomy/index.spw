# Review 2026-07-12 — merge into page-region-discoverability; “hook” remains a volatile role, not new universal anatomy.
# Plan Index — Hook Region Anatomy (Entry Call to Wonder / Action)
#
# Add a distinct, top-of-page "hook" region to landing pages as a first-class part of the documented page anatomy. This region serves as a volatile, high-value, configurable call to…

#>plan_hook_region_anatomy_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Add a distinct, top-of-page "hook" region to landing pages as a first-class part of the documented page anatomy. This region serves as a volatile, high-value, configurable call to wonder or action that: - Immediately surfaces the Spw operations, concepts, or "why this page exists" invitation that the route represents. - Rewards the vertical scrolling progression the homepage already demonstrates: the hook is the first interactive "stand" that orients attention, then hands off to deeper narrative, operators, and related routes. - Supports gestures (tap/hold/prime/resonate patterns already in use) that let visitors relate directly to context and navigate concepts — both within the page and outward to motion toward other pages. - Encourages a living relationship with pages: interacting with the hook primes semantic tokens that participate in the site's conceptual resonance system, making cross-page and intra-page navigation feel connective rather than purely link-driven. The hook is not the stable hero frame, not a generic page-hook landmark, and not only a styled "playable-hook" line. It is a profiled, inspectable region (peak attractor) that editors can make relatively volatile (swap emphasis, operator foreground, or call copy) without disturbing the deeper hero or page body. Outcome for visitors: the top of a landing page feels like a deliberate threshold you *do something with* (relate, gesture, move) rather than only read past.`
 owner = `hook-region-anatomy`
 kind = `plan`
 bucket = `css_layout_interaction`
 artifact_shape = `PLAN.md | wip.spw | index.spw`
}[reg=facet]

^"dimensions"{
 practice_depth = "reading | observation | tending"
 semantic_layer = "pragmatics"
 collaboration_phase = "tending"
 memory_tier = "hot"
 bucket = "css_layout_interaction"
}[reg=facet]

^"conceptual_model"{
 thesis = `Add a distinct, top-of-page "hook" region to landing pages as a first-class part of the documented page anatomy. This…`
 concepts = #[
  `hook`,
  `page`,
  `region`,
  `call`,
  `pages`,
 ][reg=set]
 surfaces = #[
  `js/kernel/dom-contracts.js`,
  `js/kernel/page-metadata.js`,
  `css/components/frames.css`,
  `css/effects/wonder.css`,
 ][reg=set]
 fixity_tier = `tending`
}[reg=facet]


@plans_root: ~"../../../.agents/plans/index.spw"
@plan_index_convention: ~"../../../.spw/conventions/plan-index.spw"
@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@wip: ~"./wip.spw"
@dom_contracts: ~"../../../public/js/kernel/dom-contracts.js"
@page_metadata: ~"../../../public/js/kernel/page-metadata.js"
@frames: ~"../../../public/css/components/frames.css"
@wonder: ~"../../../public/css/effects/wonder.css"
@home: ~"../../../public/css/routes/surfaces/home.css"
@site: ~"../../../public/js/site.js"
@topics_index_html: ~"../../../topics/index.html"
@about_index_html: ~"../../../about/index.html"

dispatch: .{
 plan = @plan
 wip = @wip
 dom_contracts = @dom_contracts
 page_metadata = @page_metadata
 frames = @frames
 wonder = @wonder
 home = @home
 site = @site
 topics_index_html = @topics_index_html
 about_index_html = @about_index_html
}[reg=facet]

^"research_bridge"{
 local_research = #[
  .{ label = `page_spacing_runtime` ; path = `.spw/audits/page-spacing-runtime-synthesis-2026-07.spw` }[reg=facet],
  .{ label = `vocabulary_metaphor` ; path = `.spw/audits/vocabulary-metaphor-growth-synthesis-2026-07.spw` }[reg=facet],
 ][reg=set]
 rule = `Repo-local audits/appendices/conventions — not external web.`
}[reg=facet]

^"connection_points"{
 owner_rail = `model-guided-refinement`
}[reg=facet]

^"review_disposition"{
 reviewed = `2026-07-12`
 disposition = `merge`
 merged_into = #[
  `page-region-discoverability`,
 ][reg=set]
 retain = `hook remains a volatile role`
 status = `superseded`
}[reg=facet]

^"owner_claim"{
 claim_id = "plan-hook_region_anatomy-001"
 layer = "pragmatic"
 hypothesis = `Add a distinct, top-of-page "hook" region to landing pages as a first-class part of the documented page anatomy. This region serves as a volatile, high-value, configurable call to wonder or action that: - Immediately surfaces the Spw operations, concepts, or "why this page exists" invitation that the route represents. - Rewards the vertical scrolling progression the homepage already demonstrates: the hook is the first interactive "stand" that orients attention, then hands off to deeper narrative, operators, and related routes. - Supports gestures (tap/hold/prime/resonate patterns already in use) that let visitors relate directly to context and navigate concepts — both within the page and outward to motion toward other pages. - Encourages a living relationship with pages: interacting with the hook primes semantic tokens that participate in the site's conceptual resonance system, making cross-page and intra-page navigation feel connective rather than purely link-driven. The hook is not the stable hero frame, not a generic page-hook landmark, and not only a styled "playable-hook" line. It is a profiled, inspectable region (peak attractor) that editors can make relatively volatile (swap emphasis, operator foreground, or call copy) without disturbing the deeper hero or page body. Outcome for visitors: the top of a landing page feels like a deliberate threshold you *do something with* (relate, gesture, move) rather than only read past.`
 spec_ref = ~"./PLAN.md"
 impl_ref = ~"../../../public/js/kernel/dom-contracts.js"
 probe_ref = `Author the plan first (this document).`
 falsification = `A patch cannot satisfy the public goal while leaving ../../../public/js/kernel/dom-contracts.js unchanged or untested.`
 status = "superseded"
}[reg=facet]

validation: #[
 `Author the plan first (this document).`,
 `Make the dom-contracts selector addition + CSS + page-model + site-semantics updates + 3-4 route examples.`,
 `Run after each meaningful edit:`,
 `git diff --check`,
 `node --check public/js/kernel/dom-contracts.js (and any other touched .js)`,
 `npm run check (includes audit, typecheck, CSS contracts, manifest, route runtime validation)`,
][reg=set]

editor_prompts: #[
 .{ ask = `What does hook-region-anatomy own, and what should stay out of scope?`, see = @plan }[reg=facet],
 .{ ask = `Which file should I open first to implement the next slice of hook-region-anatomy?`, see = @index }[reg=facet],
 .{ ask = `What command or check falsifies whether hook-region-anatomy still holds?`, see = @plan }[reg=facet],
][reg=set]

invariants: #[
 `Owner intent: Add a distinct, top-of-page "hook" region to landing pages as a first-class part of the documented page anatomy. This region serves as a…`,
 `PLAN.md or FIX.md sequences; index.spw routes inspection and selection.`,
 `Promote durable owner_claim patterns to .spw/slices/ or .spw/conventions/.`,
][reg=set]
