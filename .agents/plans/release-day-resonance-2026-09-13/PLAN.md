# Release-day resonance — 2026-09-13

## Public Goal

Make the September 13 A-cycle close immediately legible on Home and `/now/`, then let the existing promo/wonder surface turn that release into one useful action and one testable question.

## Kernel

- Operations: `align` for route truth; `prime` for engaged wonder.
- Fixity: tending copy and local layout; stable promo/wonder cadence contract.
- Focus: `promo-wonder-cycle`; bridge: Home and Now route openings.
- Disciplines: writer + designer; region: commons; brand physics: resonance.
- Elements: water (cadence) with fire (release action).
- Output: two route copy/layout edits, one feed/runtime cadence connection, one component-color pass.

## Boundaries

- No new wonder type, `data-spw-*` family, route, dependency, or global palette.
- Do not alter the creator-first identity, operator meanings, CSS layer order, or workbench grammar.
- Keep static Home copy useful without JavaScript; the feed only deepens it.

## Minimal Surfaces

- `index.html`: release-day opener and matching static promo/wonder fallback.
- `now/index.html`: September 13 release record, receipt-first layout, current status.
- `public/data/promo-wonder-cycle.json`: Sunday release pair and explicit practice prompt.
- `public/ts/promo-wonder-cycle.ts`: activate existing cycle cadence on the 13th/26th.
- `public/css/components/promo-wonder-cycle.css`: perspective/probe card color, action/probe CTA distinction, and cycle emphasis.
- `scripts/tests/engagement-features.test.mjs`: cadence boundary coverage.

## Validation

1. `npm run build:runtime && npm run test:engagement:run`
2. `npm run audit:copy:accessor && npm run manifest`
3. `npm run audit:module-selectors`
4. `npm run visual:checks -- --ids=home-opening,home-hook-dark`
5. `npm run check:local && git diff --check`
