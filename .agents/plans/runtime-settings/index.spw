# Review 2026-07-12 — keep as the canonical settings owner; every control needs a consumer, default, and reversible projection.
# Plan Index — runtime-settings
#
# Create a discoverable /settings/ page where visitors can quiet or hide the surface map, quiet or hide the console, and disable scroll-driven viewport activation. Taste note: this…

#>plan_runtime_settings_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Create a discoverable /settings/ page where visitors can quiet or hide the surface map, quiet or hide the console, and disable scroll-driven viewport activation. Taste note: this improves containment and calm by making the cognitive-machine chrome optable instead of unavoidable, without removing the deeper runtime features. 2026-06-30 extension: the same settings system now owns component-reward visibility (rewardDisplay) and reset access for the browser-local component collection register.`
 owner = `runtime-settings`
 kind = `plan`
 bucket = `runtime_js_validation`
 artifact_shape = `PLAN.md | wip.spw | index.spw`
}[reg=facet]

^"dimensions"{
 practice_depth = "reading | observation | tending"
 semantic_layer = "pragmatics"
 collaboration_phase = "tending"
 memory_tier = "hot"
 bucket = "runtime_js_validation"
}[reg=facet]

^"conceptual_model"{
 thesis = `Create a discoverable /settings/ page where visitors can quiet or hide the surface map, quiet or hide the console, and…`
 concepts = #[
  `settings`,
  `quiet`,
  `hide`,
  `runtime-settings`,
  `create`,
 ][reg=set]
 surfaces = #[
  `settings/index.html`,
  `js/site.js`,
  `css/style.css`,
  `js/kernel/site-settings-profiles.js`,
 ][reg=set]
 fixity_tier = `tending`
}[reg=facet]


@plans_root: ~"../../../.agents/plans/index.spw"
@plan_index_convention: ~"../../../.spw/conventions/plan-index.spw"
@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@wip: ~"./wip.spw"
@index: ~"../../../settings/index.html"
@site: ~"../../../public/js/site.js"
@style: ~"../../../public/css/style.css"
@site_settings_profiles: ~"../../../public/js/kernel/site-settings-profiles.js"
@site_settings_engine: ~"../../../public/js/kernel/site-settings-engine.js"
@site_settings_ui: ~"../../../public/js/kernel/site-settings-ui.js"
@reward_ui: ~"../../../public/js/runtime/reward-ui.js"

dispatch: .{
 plan = @plan
 wip = @wip
 index = @index
 site = @site
 style = @style
 site_settings_profiles = @site_settings_profiles
 site_settings_engine = @site_settings_engine
 site_settings_ui = @site_settings_ui
 reward_ui = @reward_ui
}[reg=facet]

^"research_bridge"{
 local_research = #[
  .{ label = `javascript_module_census` ; path = `.spw/audits/javascript-module-census-2026-07.spw` }[reg=facet],
  .{ label = `module_export_audit` ; path = `.spw/audits/module-export-standalone-2026-07.spw` }[reg=facet],
 ][reg=set]
 rule = `Repo-local audits/appendices/conventions — not external web.`
}[reg=facet]

^"absorbs"{
 reviewed = `2026-07-12`
 plans = #[
  `settings-discoverability-consolidation`,
  `settings-widgets-low-friction`,
 ][reg=set]
 rule = `Absorbed by 2026-07-12 review; see each source review_disposition and archive/2026-07-12-review-execution.md.`
}[reg=facet]

^"owner_claim"{
 claim_id = "plan-runtime_settings-001"
 layer = "pragmatic"
 hypothesis = `Create a discoverable /settings/ page where visitors can quiet or hide the surface map, quiet or hide the console, and disable scroll-driven viewport activation. Taste note: this improves containment and calm by making the cognitive-machine chrome optable instead of unavoidable, without removing the deeper runtime features. 2026-06-30 extension: the same settings system now owns component-reward visibility (rewardDisplay) and reset access for the browser-local component collection register.`
 spec_ref = ~"./PLAN.md"
 impl_ref = ~"../../../settings/index.html"
 probe_ref = `Hypotheses: visitors can reduce intrusive runtime chrome without losing route access.`
 falsification = `A patch cannot satisfy the public goal while leaving ../../../settings/index.html unchanged or untested.`
 status = "active"
}[reg=facet]

scope: .{
 in = #[
  `In scope: localStorage-backed settings, settings page markup, navigator/console/core hooks, CSS preference states, PWA cache registration.`,
  `In scope: canonical display settings for floating reward chrome and persistence-register reset hooks for browser-local runtime memories.`,
  `-`,
 ][reg=set]
}[reg=facet]

validation: #[
 `Hypotheses: visitors can reduce intrusive runtime chrome without losing route access.`,
 `Negative controls: RPG Wednesday local gameplay changes and existing optional feature loading still work.`,
 `Demo sequence: visit /settings/, choose quiet/hidden preferences, reload home/software pages, confirm runtime chrome follows preferences.`,
][reg=set]

editor_prompts: #[
 .{ ask = `What does runtime-settings own, and what should stay out of scope?`, see = @plan }[reg=facet],
 .{ ask = `Which file should I open first to implement the next slice of runtime-settings?`, see = @index }[reg=facet],
 .{ ask = `What command or check falsifies whether runtime-settings still holds?`, see = @plan }[reg=facet],
][reg=set]

invariants: #[
 `Owner intent: Create a discoverable /settings/ page where visitors can quiet or hide the surface map, quiet or hide the console, and disable…`,
 `PLAN.md or FIX.md sequences; index.spw routes inspection and selection.`,
 `Promote durable owner_claim patterns to .spw/slices/ or .spw/conventions/.`,
][reg=set]
