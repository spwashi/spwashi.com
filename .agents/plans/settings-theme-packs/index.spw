# Plan Index — settings-theme-packs
#
# Make the existing palette chooser surfaces act on canonical runtime state, and expand the site with authored theme families that each have distinct light and dark variants.

#>plan_settings_theme_packs_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Make the existing palette chooser surfaces act on canonical runtime state, and expand the site with authored theme families that each have distinct light and dark variants.`
 owner = `settings-theme-packs`
 kind = `plan`
 bucket = `active_backlog`
 artifact_shape = `PLAN.md | index.spw`
}[reg=facet]

^"dimensions"{
 practice_depth = "reading | observation | tending"
 semantic_layer = "pragmatics"
 collaboration_phase = "tending"
 memory_tier = "hot"
 bucket = "active_backlog"
}[reg=facet]

@plans_root: ~"../../../.agents/plans/index.spw"
@plan_index_convention: ~"../../../.spw/conventions/plan-index.spw"
@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@settings_index_html: ~"../../../settings/index.html"
@design_index_html: ~"../../../design/index.html"
@design_palettes_index_html: ~"../../../design/palettes/index.html"
@settings_state: ~"../../../.spw/reviews/runtime-audit/settings-state.spw"

dispatch: .{
 plan = @plan
 settings_index_html = @settings_index_html
 design_index_html = @design_index_html
 design_palettes_index_html = @design_palettes_index_html
 settings_state = @settings_state
}[reg=facet]

^"owner_claim"{
 claim_id = "plan-settings_theme_packs-001"
 layer = "pragmatic"
 hypothesis = `Make the existing palette chooser surfaces act on canonical runtime state, and expand the site with authored theme families that each have distinct light and dark variants.`
 spec_ref = ~"./PLAN.md"
 impl_ref = ~"../../../settings/index.html"
 probe_ref = `rg 'settings-theme-packs' .agents/plans/settings-theme-packs`
 falsification = `A patch cannot satisfy the public goal while leaving ../../../settings/index.html unchanged or untested.`
 status = "active"
}[reg=facet]

editor_prompts: #[
 .{ ask = `What does settings-theme-packs own, and what should stay out of scope?`, see = @plan }[reg=facet],
 .{ ask = `Which file should I open first to implement the next slice of settings-theme-packs?`, see = @index }[reg=facet],
][reg=set]

invariants: #[
 `Owner intent: Make the existing palette chooser surfaces act on canonical runtime state, and expand the site with authored theme families that each have…`,
 `PLAN.md or FIX.md sequences; index.spw routes inspection and selection.`,
 `Promote durable owner_claim patterns to .spw/slices/ or .spw/conventions/.`,
][reg=set]

