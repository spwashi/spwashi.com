# Review 2026-07-12 — keep as the source-tree audit rail; topology must be observed before another rename or move.
# Plan Index — js-surface-ecology
#
# Make the shape of public/js legible before attempting deep refactors. The desired end state is a tree where a new reader can distinguish durable primitives, semantic machinery,…

#>plan_js_surface_ecology_index
#:index #!plan
#:operation #!prime
#:layer #!pragmatics

operation = "prime"
fixity = "tending"

intent: .{
 goal = `Make the shape of public/js legible before attempting deep refactors. The desired end state is a tree where a new reader can distinguish durable primitives, semantic machinery, runtime behavior, interface affordances, and feature modules at a glance.`
 owner = `js-surface-ecology`
 kind = `plan`
 bucket = `runtime_js_validation`
 artifact_shape = `PLAN.md | index.spw`
}[reg=facet]

^"dimensions"{
 practice_depth = "reading | observation | tending"
 semantic_layer = "pragmatics"
 collaboration_phase = "tending"
 memory_tier = "hot"
 bucket = "runtime_js_validation"
}[reg=facet]

^"conceptual_model"{
 thesis = `Make the shape of public/js legible before attempting deep refactors. The desired end state is a tree where a new…`
 concepts = #[
  `js-surface-ecology`,
  `shape`,
  `legible`,
  `attempting`,
  `deep`,
 ][reg=set]
 surfaces = #[
  `js`,
  `js/site.js`,
  `js/typed/`,
  `css/style.css`,
 ][reg=set]
 fixity_tier = `tending`
}[reg=facet]


@plans_root: ~"../../../.agents/plans/index.spw"
@plan_index_convention: ~"../../../.spw/conventions/plan-index.spw"
@wip_notebook: ~"../../../.spw/conventions/wip-notebook.spw"
@plan: ~"./PLAN.md"
@js: ~"../../../public/js"
@site: ~"../../../public/js/site.js"
@typed: ~"../../../public/js/typed/"
@style: ~"../../../public/css/style.css"
@interface: ~"../../../public/js/interface/"

dispatch: .{
 plan = @plan
 js = @js
 site = @site
 typed = @typed
 style = @style
 interface = @interface
}[reg=facet]

^"research_bridge"{
 local_research = #[
  .{ label = `javascript_module_census` ; path = `.spw/audits/javascript-module-census-2026-07.spw` }[reg=facet],
  .{ label = `module_export_audit` ; path = `.spw/audits/module-export-standalone-2026-07.spw` }[reg=facet],
 ][reg=set]
 rule = `Repo-local audits/appendices/conventions — not external web.`
}[reg=facet]

^"absorbs"{
 reviewed = `2026-07-12`
 plans = #[
  `javascript-standalone-utility`,
  `js-taxonomy-cleanup`,
 ][reg=set]
 rule = `Absorbed by 2026-07-12 review; see each source review_disposition and archive/2026-07-12-review-execution.md.`
}[reg=facet]

^"owner_claim"{
 claim_id = "plan-js_surface_ecology-001"
 layer = "pragmatic"
 hypothesis = `Make the shape of public/js legible before attempting deep refactors. The desired end state is a tree where a new reader can distinguish durable primitives, semantic machinery, runtime behavior, interface affordances, and feature modules at a glance.`
 spec_ref = ~"./PLAN.md"
 impl_ref = ~"../../../public/js"
 probe_ref = `constrain inspection to public/js`
 falsification = `A patch cannot satisfy the public goal while leaving ../../../public/js unchanged or untested.`
 status = "active"
}[reg=facet]

scope: .{
 in = #[
  `In scope for the first pass: inspect public/js only, ignore generated/build-heavy directories, classify files by responsibility, move obvious files, update import paths, and stop.`,
  `-`,
 ][reg=set]
}[reg=facet]

validation: #[
 `constrain inspection to public/js`,
 `classify before merging`,
 `preserve suspicious files in place or move them to legacy/`,
 `verify updated import paths after each small move set`,
][reg=set]

editor_prompts: #[
 .{ ask = `What does js-surface-ecology own, and what should stay out of scope?`, see = @plan }[reg=facet],
 .{ ask = `Which file should I open first to implement the next slice of js-surface-ecology?`, see = @index }[reg=facet],
 .{ ask = `What command or check falsifies whether js-surface-ecology still holds?`, see = @plan }[reg=facet],
][reg=set]

invariants: #[
 `Owner intent: Make the shape of public/js legible before attempting deep refactors. The desired end state is a tree where a new reader can distinguish…`,
 `PLAN.md or FIX.md sequences; index.spw routes inspection and selection.`,
 `Promote durable owner_claim patterns to .spw/slices/ or .spw/conventions/.`,
][reg=set]
