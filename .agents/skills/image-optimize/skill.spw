# image-optimize — skill surface
#
# Generated contents below; hand-authored facets go underneath.
# See .agents/skills/_shared/skill-surface.spw for the shape.

# <<< generated by scripts/spw-skill-surface.mjs — edits above this line are kept
#>skill_image_optimize
#:skill #!agents
#:layer #!pragmatics

~#describes: "Generate lighter public image derivatives when promoting assets. Skip for throwaway experiments."

@prose: ~"./SKILL.md"
@references: ~"./references"
@scripts: ~"./scripts"

^"contents"{
  "SKILL.md": .{ role = `prose — the instructions a model reads` }[reg=facet]
  "references/": .{ role = `reference material the skill cites`, holds = #[`size-tiers.spw`][reg=set] }[reg=facet]
  "scripts/": .{ role = `executable helpers the skill invokes`, holds = #[`generate-variants.sh`][reg=set] }[reg=facet]
}[reg=facet]
# >>> end generated
