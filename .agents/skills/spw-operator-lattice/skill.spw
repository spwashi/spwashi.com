# spw-operator-lattice — skill surface
#
# Generated contents below; hand-authored facets go underneath.
# See .agents/skills/_shared/skill-surface.spw for the shape.

# <<< generated by scripts/spw-skill-surface.mjs — edits above this line are kept
#>skill_spw_operator_lattice
#:skill #!agents
#:layer #!pragmatics

~#describes: "Inspect Spw operator usage across .spw, HTML data-spw-*, and route links. Use for operator/cross-link audits—not to sprinkle more chips everywhere."

@prose: ~"./SKILL.md"

^"contents"{
  "SKILL.md": .{ role = `prose — the instructions a model reads` }[reg=facet]
}[reg=facet]
# >>> end generated

expression = lattice[audit]{operator.apposition.link}<corpus>

^"value"{
  standing = blocked
  worth: `The tool reports an empty field as though it were a finding.`
  measured: .{
    apposition_cells_read = 6
    surfaces_scanned = 237
    appositions_present = "thousands"
  }[reg=facet]
  next: `Either the corpus adopts ~#name(body) or the tool learns the ~#name: form the tree already uses. Both are one pass.`
  ~<../../../.spw/skills/semantics-rigor.spw>
}
