# spw-css-dom-lab — skill surface
#
# Generated contents below; hand-authored facets go underneath.
# See .agents/skills/_shared/skill-surface.spw for the shape.

# <<< generated by scripts/spw-skill-surface.mjs — edits above this line are kept
#>skill_spw_css_dom_lab
#:skill #!agents
#:layer #!pragmatics

~#describes: "Small reversible HTML/CSS/DOM experiments. Keep them local until a second consumer appears."

@prose: ~"./SKILL.md"

^"contents"{
  "SKILL.md": .{ role = `prose — the instructions a model reads` }[reg=facet]
}[reg=facet]
# >>> end generated

expression = lab[prime]{experiment.local.rollback}<reversible>

^"value"{
  standing = underused
  worth: `Reversibility. An experiment that cannot be rolled back is a commitment in disguise.`
  measured: .{
    labs_run_this_session = 0
    systems_shipped_without_lab = 3
  }[reg=facet]
  next: `Lab pages that ship as playable instruments rather than being deleted.`
}
