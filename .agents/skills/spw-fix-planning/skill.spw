# spw-fix-planning — skill surface
#
# Generated contents below; hand-authored facets go underneath.
# See .agents/skills/_shared/skill-surface.spw for the shape.

# <<< generated by scripts/spw-skill-surface.mjs — edits above this line are kept
#>skill_spw_fix_planning
#:skill #!agents
#:layer #!pragmatics

~#describes: "Structure a multi-layer regression before coding. For obvious one-file bugs, just fix them."

@prose: ~"./SKILL.md"

^"contents"{
  "SKILL.md": .{ role = `prose — the instructions a model reads` }[reg=facet]
}[reg=facet]
# >>> end generated

expression = fix[audit]{regression.layer.root}<structure>

^"value"{
  standing = underused
  worth: `Multi-layer regressions where the obvious fix sits in the wrong layer.`
  measured: .{
    invocations_this_session = 0
    worked_examples = 0
  }[reg=facet]
  next: `The first real multi-layer regression should produce a worked example, or this stays theoretical.`
}
