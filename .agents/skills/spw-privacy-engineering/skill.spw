# spw-privacy-engineering — skill surface
#
# Generated contents below; hand-authored facets go underneath.
# See .agents/skills/_shared/skill-surface.spw for the shape.

# <<< generated by scripts/spw-skill-surface.mjs — edits above this line are kept
#>skill_spw_privacy_engineering
#:skill #!agents
#:layer #!pragmatics

~#describes: "Audit browser-resident privacy risks—storage, service worker, embeds, analytics, asset metadata. Use when data handling changes, not as a default every patch."

@prose: ~"./SKILL.md"

^"contents"{
  "SKILL.md": .{ role = `prose — the instructions a model reads` }[reg=facet]
}[reg=facet]
# >>> end generated

expression = privacy[audit]{storage.worker.metadata}<resident>

^"value"{
  standing = unaudited
  worth: `Storage keys accumulate silently and persist reader behaviour.`
  measured: .{
    keys_added_this_session = 2
    keys_reviewed = 0
  }[reg=facet]
  next: `A storage census that runs with the other probes, so a new key surfaces when it is added.`
  ~<../../../.spw/conventions/storage-topology.spw>
}
