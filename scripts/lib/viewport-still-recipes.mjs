/**
 * Viewport still recipes — the still a person would take, not the region's
 * document box. Tooling-only; do not ship these through the public runtime
 * catalog. Region ecology fixtures remain the seat registry.
 *
 * clipSpace is always the device frame after prepare + scroll.
 */

export const VIEWPORT_STILL_RECIPES = Object.freeze([
  Object.freeze({
    id: 'home-opening',
    fixtureId: 'home-hook',
    label: 'Home opening',
    specimenRoute: '/',
    selector: '#home-frame',
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Identity leads. The working view is a local instrument, not the subject.',
    captureValue: 'One pocket frame: person, then the working view, then the first motifs.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/routes/surfaces/home.css',
      'public/css/shell/chrome/adaptive.css',
      'public/css/shell/chrome/section-context.css',
      'public/css/handles/operators/state-semantics.css',
    ]),
  }),
  Object.freeze({
    id: 'home-reasons',
    fixtureId: 'home-hook',
    label: 'Home reasons',
    specimenRoute: '/',
    selector: '#home-reason-why',
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Three charges may share a fold, but they must not read as one unlabeled stack.',
    captureValue: 'Why / readers / takeaway as the visible subjects after the opening.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/routes/surfaces/home.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entrance-closed',
    fixtureId: 'home-hook',
    label: 'Home entrance closed',
    specimenRoute: '/#choose-your-entrance',
    selector: '#choose-your-entrance',
    prepare: Object.freeze({
      close: Object.freeze(['.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Pick your door is the subject. Encyclopedic atlas stays deferred.',
    captureValue: 'Closed disclosure: role hooks visible, deeper atlas not in the first frame.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/routes/surfaces/home.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entrance-open',
    fixtureId: 'home-hook',
    label: 'Home entrance open',
    specimenRoute: '/',
    selector: '#choose-your-entrance .home-depth-disclosure',
    prepare: Object.freeze({
      open: Object.freeze(['.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Opening the atlas must change the still. If the first frame is unchanged, the disclosure is below the fold.',
    captureValue: 'Open disclosure: resonance and image atlas become the next subject, not a surprise dump.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/routes/surfaces/home.css',
    ]),
  }),
  Object.freeze({
    id: 'home-nav-open',
    fixtureId: 'home-hook',
    label: 'Home nav open',
    specimenRoute: '/',
    selector: '.site-header[data-spw-menu="open"]',
    prepare: Object.freeze({
      click: Object.freeze(['.spw-nav-toggle']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Opening Routes must cover the first fold. If the still matches rest, the toggle is decoration.',
    captureValue: 'Pocket/fold menu: authored rooms stay reachable; overlay does not clip the toggle.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/runtime/shell-disclosure.js',
      'public/css/shell/chrome/navigation.css',
    ]),
  }),
  Object.freeze({
    id: 'home-living-term-note',
    fixtureId: 'home-hook',
    label: 'Home living-term note',
    specimenRoute: '/',
    selector: '.spw-concept-popover',
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
      click: Object.freeze(['#home-frame .spw-living-term[data-spw-concept="living-concepts"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Tap inspect must leave a visible note. If the still is only the lede, the popup never opened.',
    captureValue: 'Concept note over the home opening: gather and dismiss reachable.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/interface/haptics.js',
      'public/css/grammar/syntax.css',
    ]),
  }),
  Object.freeze({
    id: 'home-search-open',
    fixtureId: 'home-hook',
    label: 'Home search open',
    specimenRoute: '/',
    selector: '[data-spw-site-search="open"]',
    prepare: Object.freeze({
      click: Object.freeze(['[data-spw-site-search-open]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Search must occupy the fold as a dialog. If the still is the opening lede, the trigger never opened it.',
    captureValue: 'Site-search dialog from the header Search control.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/runtime/site-search.js',
      'public/css/shell/chrome/navigation.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-loops',
    fixtureId: 'home-entry-loops',
    label: 'Home entry loops',
    specimenRoute: '/#entry-loops',
    selector: '#entry-loops',
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Four .spw-card tiles must pack as a grid. If the still is one lonely card, the public noun never sat.',
    captureValue: 'Card primitive in the home loops: illustrated tiles, sigils, and bodies.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/cards.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-panels',
    fixtureId: 'home-entry-panels',
    label: 'Home entry panels',
    specimenRoute: '/#home-entry-panels',
    selector: '#home-entry-panels',
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Panels stay matte callouts with chips inside. If they read as a second card grid, the kind drifted.',
    captureValue: 'Panel primitive: request tiles and operator chips on the home frame.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/surfaces.css',
      'public/css/handles/operators/sigils-and-chips.css',
    ]),
  }),
  Object.freeze({
    id: 'home-lede',
    fixtureId: 'home-hook',
    label: 'Home lede',
    specimenRoute: '/',
    selector: '#home-frame-note',
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'The lede is manuscript prose. If the crop is the working view, the textual role never sat.',
    captureValue: 'Prose primitive: home.hook.lede naming the rooms.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/typography/typesetting.css',
    ]),
  }),
  Object.freeze({
    id: 'home-chips',
    fixtureId: 'home-hook',
    label: 'Home chips',
    specimenRoute: '/',
    selector: '#home-hook .mode-panel:not([hidden])',
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Operator chips in the open working view must stay 44px and wrap. If the still is only the lens row, the chip never sat.',
    captureValue: 'Chip primitive: Study panel with .spw-chip handles.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/handles/operators/sigils-and-chips.css',
    ]),
  }),
  Object.freeze({
    id: 'home-pronunciation',
    fixtureId: 'home-hook',
    label: 'Home pronunciation hint',
    specimenRoute: '/',
    selector: '.spw-pronunciation-hint',
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
      charge: Object.freeze(['#home-frame .frame-sigil[data-spw-operator="frame"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Charging the frame sigil must leave a say/mnemonic hint. If the still is only the brace, the hint never mounted.',
    captureValue: 'Pronunciation hint over the home frame sigil.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/interface/pronunciation.js',
      'public/css/handles/operators/adaptive.css',
    ]),
  }),
  Object.freeze({
    id: 'home-region-menu',
    fixtureId: 'home-hook',
    label: 'Home region menu',
    specimenRoute: '/',
    selector: '.spw-region-menu',
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
      contextmenu: Object.freeze(['#home-frame .frame-sigil[data-spw-operator="frame"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Inspect on the frame sigil must open the region menu. If the still is the opening lede, the menu never sat.',
    captureValue: 'Region-menu popover from the home frame handle.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/runtime/region-menu.js',
    ]),
  }),
  Object.freeze({
    id: 'home-topic-note',
    fixtureId: 'home-cluster',
    label: 'Home topic note',
    specimenRoute: '/#reading-layers',
    selector: '.spw-topic-popover',
    prepare: Object.freeze({
      hold: Object.freeze(['#reading-layers .spw-topic[data-spw-topic="topic"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'A long-press on a topic handle must leave a context note. If the still is only the layers essay, the popover never opened.',
    captureValue: 'Topic-discovery note over the reading-layers topic handle.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/interface/topic-discovery.js',
      'public/css/grammar/syntax.css',
    ]),
  }),
  Object.freeze({
    id: 'about-opening',
    fixtureId: 'about-hook',
    label: 'About opening',
    specimenRoute: '/about/',
    selector: '#about-frame',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Person first, then the italic creator sentence, then the lens. Anatomy chrome should not steal the crop.',
    captureValue: 'Identity, tagline, then reading lens.',
    sourceFiles: Object.freeze([
      'about/index.html',
      'public/css/routes/surfaces/about.css',
      'public/css/ornament/ornament.css',
    ]),
  }),
  Object.freeze({
    id: 'town-opening',
    fixtureId: 'town-hook',
    label: 'Town opening',
    specimenRoute: '/town/',
    selector: '#town-atlas-frame',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'The atlas diagram is the subject. Origin prose stays below the still.',
    captureValue: 'Visit/world lens, labeled rooms, climate rail.',
    sourceFiles: Object.freeze([
      'town/index.html',
      'public/css/systems/svg-surfaces.css',
      'public/css/ornament/ornament.css',
    ]),
  }),
  Object.freeze({
    id: 'research-opening',
    fixtureId: 'research-hook',
    label: 'Research opening',
    specimenRoute: '/research/',
    selector: '#research-frame',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Ladder rungs should name signal → record. The photo is atmosphere, not the subject.',
    captureValue: 'Cite/decide lens, insight ladder, tropes rail.',
    sourceFiles: Object.freeze([
      'research/index.html',
      'public/css/systems/svg-surfaces.css',
      'public/css/ornament/ornament.css',
    ]),
  }),
  Object.freeze({
    id: 'membership-opening',
    fixtureId: 'membership-hook',
    label: 'Membership opening',
    specimenRoute: '/membership/',
    selector: '#membership-frame',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Roles around the issue bed should be nameable without the membership essay.',
    captureValue: 'Read/join lens, role-field SVG, climate rail.',
    sourceFiles: Object.freeze([
      'membership/index.html',
      'public/css/systems/svg-surfaces.css',
      'public/css/ornament/ornament.css',
    ]),
  }),
  Object.freeze({
    id: 'svg-storytelling-opening',
    fixtureId: 'svg-storytelling-hook',
    label: 'SVG storytelling opening',
    specimenRoute: '/topics/craft/svg/',
    selector: '#svg-storytelling',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Glyph, creature, divider, token stay named in the kit still.',
    captureValue: 'Motif kit plus material rail.',
    sourceFiles: Object.freeze([
      'topics/craft/svg/index.html',
      'public/css/systems/svg-surfaces.css',
      'public/css/ornament/ornament.css',
    ]),
  }),
  Object.freeze({
    id: 'curriculum-opening',
    fixtureId: 'curriculum-hook',
    label: 'Curriculum opening',
    specimenRoute: '/curriculum/',
    selector: '#curriculum-hero',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Spwashi teaches: one mechanism, one test, one artifact. Math and software neighbors stay named.',
    captureValue: 'Curriculum hook still: creator-first title, living terms, three practice beds.',
    sourceFiles: Object.freeze([
      'curriculum/index.html',
      'public/css/tokens/core.css',
      'public/css/typography/typesetting.css',
    ]),
  }),
  Object.freeze({
    id: 'software-opening',
    fixtureId: 'software-hook',
    label: 'Software opening',
    specimenRoute: '/topics/software/',
    selector: '#software-surface',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'The CS half of the curriculum. Grammar, not a baker\'s-dozen hook.',
    captureValue: 'Software hook still: curriculum neighbor, named owners, step-trace living term.',
    sourceFiles: Object.freeze([
      'topics/software/index.html',
      'public/css/tokens/core.css',
    ]),
  }),
  Object.freeze({
    id: 'math-opening',
    fixtureId: 'math-hook',
    label: 'Math opening',
    specimenRoute: '/topics/math/',
    selector: '#math-hero',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Structure atlas as the math neighbor. Dose and receipt stay countable.',
    captureValue: 'Math hook still: study path and curriculum/recipes chips.',
    sourceFiles: Object.freeze([
      'topics/math/index.html',
      'public/css/tokens/core.css',
    ]),
  }),
  Object.freeze({
    id: 'folio-fold-rest',
    fixtureId: 'folio-fold-probe',
    label: 'Folio fold rest',
    specimenRoute: '/design/folios/#folio-fold-probe',
    selector: '#folio-fold-probe',
    prepare: Object.freeze({
      check: Object.freeze(['#folio-fold-probe input[value="rest"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'At rest the fold is only an edge. The radio row must remain a 44px instrument.',
    captureValue: 'Kinetic study component probe, rest state, JavaScript off.',
    sourceFiles: Object.freeze([
      'design/folios/index.html',
      'public/css/routes/surfaces/design.css',
    ]),
  }),
  Object.freeze({
    id: 'folio-fold-open',
    fixtureId: 'folio-fold-probe',
    label: 'Folio fold open',
    specimenRoute: '/design/folios/#folio-fold-probe',
    selector: '#folio-fold-probe',
    prepare: Object.freeze({
      check: Object.freeze(['#folio-fold-probe input[value="open"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Checking Open must change the still. If rest and open match, the probe is decoration.',
    captureValue: 'Kinetic study component probe after the open radio is checked.',
    sourceFiles: Object.freeze([
      'design/folios/index.html',
      'public/css/routes/surfaces/design.css',
    ]),
  }),
  Object.freeze({
    id: 'folio-growth-rest',
    fixtureId: 'folio-growth-probe',
    label: 'Folio growth rest',
    specimenRoute: '/design/folios/#folio-growth-probe',
    selector: '#folio-growth-probe',
    prepare: Object.freeze({
      check: Object.freeze(['#folio-growth-probe input[value="rest"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'A repeated unit, still. Outer rings should recede until Branch is chosen.',
    captureValue: 'Botanical metabiology probe, rest state.',
    sourceFiles: Object.freeze([
      'design/folios/index.html',
      'public/css/routes/surfaces/design.css',
    ]),
  }),
  Object.freeze({
    id: 'folio-growth-branch',
    fixtureId: 'folio-growth-probe',
    label: 'Folio growth branch',
    specimenRoute: '/design/folios/#folio-growth-probe',
    selector: '#folio-growth-probe',
    prepare: Object.freeze({
      check: Object.freeze(['#folio-growth-probe input[value="branch"]']),
    }),
    layoutScenarios: Object.freeze(['pocket', 'fold']),
    wonder: 'Branching should occupy room. If the organism looks unchanged, the growth rule is not visible.',
    captureValue: 'Botanical metabiology probe after Branch is checked.',
    sourceFiles: Object.freeze([
      'design/folios/index.html',
      'public/css/routes/surfaces/design.css',
    ]),
  }),
  Object.freeze({
    id: 'folio-prices',
    fixtureId: 'folio-prices',
    label: 'Folio price ladder',
    specimenRoute: '/design/folios/#folio-prices',
    selector: '#folio-prices',
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Planning amounts must read as a ladder, not as a live cart.',
    captureValue: 'Visible $35 / $75 / $150 asks with Seed 3-for-$95 and quoted prints.',
    sourceFiles: Object.freeze([
      'design/folios/index.html',
      'public/css/routes/surfaces/design.css',
    ]),
  }),
]);

export const VIEWPORT_STILL_CHECKS = Object.freeze([
  Object.freeze({
    id: 'home-entry-loops-pin',
    fixtureId: 'home-entry-loops',
    label: 'Home entry loops pin',
    specimenRoute: '/#entry-loops',
    selector: '#entry-loops',
    attention: Object.freeze({ section: 'entry-loops' }),
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'A deep link pins the authored loops, not the nested hook.',
    captureValue: 'Attention architecture: fragment + pin outrank the viewport guess.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/runtime/attention/section-handle.js',
      'public/js/runtime/attention/capture-pins.js',
    ]),
  }),
  Object.freeze({
    id: 'about-opening-dark',
    fixtureId: 'about-hook',
    label: 'About opening dark',
    specimenRoute: '/about/',
    selector: '#about-frame',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Explicit dark about palette, not a comma-joined @media selector.',
    captureValue: 'Theming check: data-spw-color-mode=dark on the about surface.',
    sourceFiles: Object.freeze([
      'about/index.html',
      'public/css/routes/surfaces/about.css',
    ]),
  }),
  Object.freeze({
    id: 'topics-opening-dark',
    fixtureId: 'topics-register',
    label: 'Topics opening dark',
    specimenRoute: '/topics/',
    selector: '#topics-register',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket', 'fold', 'broadsheet']),
    wonder: 'Explicit dark topics palette stays on the register, not a dropped selector.',
    captureValue: 'Theming check: data-spw-color-mode=dark on the topics surface.',
    sourceFiles: Object.freeze([
      'topics/index.html',
      'public/css/routes/surfaces/topics.css',
    ]),
  }),
  Object.freeze({
    id: 'home-opening-reduced',
    fixtureId: 'home-hook',
    label: 'Home opening reduced motion',
    specimenRoute: '/',
    selector: '#home-frame',
    conditions: Object.freeze({ reducedMotion: 'reduce' }),
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Reduced motion still has a decisive opening subject without relying on animation peaks.',
    captureValue: 'Environment check: prefers-reduced-motion reduce on the home opening.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/routes/surfaces/home.css',
    ]),
  }),
  Object.freeze({
    id: 'home-hook-dark',
    fixtureId: 'home-hook',
    label: 'Home hook dark',
    specimenRoute: '/',
    selector: '#home-hook',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Wash slice and working-view ink stay readable on the dark paper ground.',
    captureValue: 'Theming check: dark color-mode on the home hook with a wash texture slice.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/effects/texture-slice.css',
      'public/css/tokens/core.css',
    ]),
  }),
  Object.freeze({
    id: 'home-hook-high-contrast',
    fixtureId: 'home-hook',
    label: 'Home hook high contrast',
    specimenRoute: '/',
    selector: '#home-hook',
    conditions: Object.freeze({ highContrast: 'on' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'High contrast must keep hook copy stronger than the wash overlay.',
    captureValue: 'Theming check: high-contrast on the home hook texture slice.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/effects/texture-slice.css',
      'public/css/tokens/core.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-loops-dark',
    fixtureId: 'home-entry-loops',
    label: 'Home entry loops dark',
    specimenRoute: '/#entry-loops',
    selector: '#entry-loops',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Card tiles keep title and sigil on the dark paper. If the still is a black slab, ink did not follow the noun.',
    captureValue: 'Card primitive in dark color-mode.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/cards.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-loops-reduced',
    fixtureId: 'home-entry-loops',
    label: 'Home entry loops reduced motion',
    specimenRoute: '/#entry-loops',
    selector: '#entry-loops',
    conditions: Object.freeze({ reducedMotion: 'reduce' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'The four loops stay a grid without motion peaks. If a card vanishes, reduced-motion hid the primitive.',
    captureValue: 'Card primitive under prefers-reduced-motion.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/cards.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-loops-ember',
    fixtureId: 'home-entry-loops',
    label: 'Home entry loops banked ember',
    specimenRoute: '/#entry-loops',
    selector: '#entry-loops',
    conditions: Object.freeze({ themePack: 'banked-ember' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Ember cannot melt the card grid into the shell. Sigils stay readable.',
    captureValue: 'Card primitive on banked-ember.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/cards.css',
      'public/css/themes/packs.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-loops-high-contrast',
    fixtureId: 'home-entry-loops',
    label: 'Home entry loops high contrast',
    specimenRoute: '/#entry-loops',
    selector: '#entry-loops',
    conditions: Object.freeze({ highContrast: 'on' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'High contrast must keep card body copy stronger than the illustration wash.',
    captureValue: 'Card primitive under high-contrast.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/cards.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-panels-dark',
    fixtureId: 'home-entry-panels',
    label: 'Home entry panels dark',
    specimenRoute: '/#home-entry-panels',
    selector: '#home-entry-panels',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Matte panels and chips stay on the dark ground. If chips vanish, the panel lost its handles.',
    captureValue: 'Panel primitive in dark color-mode.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/surfaces.css',
      'public/css/handles/operators/sigils-and-chips.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entrance-ember',
    fixtureId: 'home-hook',
    label: 'Home entrance banked ember',
    specimenRoute: '/#choose-your-entrance',
    selector: '#choose-your-entrance',
    conditions: Object.freeze({ themePack: 'banked-ember' }),
    prepare: Object.freeze({
      close: Object.freeze(['.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Door titles stay ink on ember tiles. If only chips remain, the hook heading washed out.',
    captureValue: 'Role-hook primitive on banked-ember: title, body, chips.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/cards.css',
      'public/css/themes/packs.css',
    ]),
  }),
  Object.freeze({
    id: 'home-entry-panels-ember',
    fixtureId: 'home-entry-panels',
    label: 'Home entry panels banked ember',
    specimenRoute: '/#home-entry-panels',
    selector: '#home-entry-panels',
    conditions: Object.freeze({ themePack: 'banked-ember' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Ember warmth cannot turn a panel into a glass card. Chips stay inside the callout.',
    captureValue: 'Panel primitive on banked-ember.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/components/surfaces.css',
      'public/css/themes/packs.css',
    ]),
  }),
  Object.freeze({
    id: 'home-lede-dark',
    fixtureId: 'home-hook',
    label: 'Home lede dark',
    specimenRoute: '/',
    selector: '#home-frame-note',
    conditions: Object.freeze({ colorMode: 'dark' }),
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Lede links stay room-names on dark paper, not washed chrome.',
    captureValue: 'Prose primitive in dark color-mode.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/typography/typesetting.css',
    ]),
  }),
  Object.freeze({
    id: 'home-chips-dark',
    fixtureId: 'home-hook',
    label: 'Home chips dark',
    specimenRoute: '/',
    selector: '#home-hook .mode-panel:not([hidden])',
    conditions: Object.freeze({ colorMode: 'dark' }),
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Chip sigils keep operator color on dark. If they flatten to one ink, the handle died.',
    captureValue: 'Chip primitive in dark color-mode.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/handles/operators/sigils-and-chips.css',
    ]),
  }),
  Object.freeze({
    id: 'home-pronunciation-dark',
    fixtureId: 'home-hook',
    label: 'Home pronunciation hint dark',
    specimenRoute: '/',
    selector: '.spw-pronunciation-hint',
    conditions: Object.freeze({ colorMode: 'dark' }),
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
      charge: Object.freeze(['#home-frame .frame-sigil[data-spw-operator="frame"]']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'The say/mnemonic hint stays ink on dark paper. If it vanishes, capture hid status chrome.',
    captureValue: 'Pronunciation hint in dark color-mode.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/interface/pronunciation.js',
      'public/css/handles/operators/adaptive.css',
    ]),
  }),
  Object.freeze({
    id: 'home-region-menu-dark',
    fixtureId: 'home-hook',
    label: 'Home region menu dark',
    specimenRoute: '/',
    selector: '.spw-region-menu',
    conditions: Object.freeze({ colorMode: 'dark' }),
    prepare: Object.freeze({
      close: Object.freeze(['.home-field-notes', '.home-depth-disclosure']),
      contextmenu: Object.freeze(['#home-frame .frame-sigil[data-spw-operator="frame"]']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Region menu copy stays on the dark shell. If the still is the lede, capture hid role=menu chrome.',
    captureValue: 'Region-menu popover in dark color-mode.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/runtime/region-menu.js',
    ]),
  }),
  Object.freeze({
    id: 'home-topic-note-dark',
    fixtureId: 'home-cluster',
    label: 'Home topic note dark',
    specimenRoute: '/#reading-layers',
    selector: '.spw-topic-popover',
    conditions: Object.freeze({ colorMode: 'dark' }),
    prepare: Object.freeze({
      hold: Object.freeze(['#reading-layers .spw-topic[data-spw-topic="topic"]']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Topic context stays readable on dark paper. If the still is the essay, the hold never opened it.',
    captureValue: 'Topic-discovery note in dark color-mode.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/js/interface/topic-discovery.js',
      'public/css/grammar/syntax.css',
    ]),
  }),
  Object.freeze({
    id: 'home-hook-banked-ember',
    fixtureId: 'home-hook',
    label: 'Home hook banked ember',
    specimenRoute: '/',
    selector: '#home-hook',
    conditions: Object.freeze({ themePack: 'banked-ember' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Ember’s dark shell cannot steal hook ink or blow out the wash tile.',
    captureValue: 'Theming check: banked-ember pack on the home hook.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/themes/packs.css',
      'public/css/effects/texture-slice.css',
      'public/css/systems/image-theming.css',
    ]),
  }),
  Object.freeze({
    id: 'home-hook-ember-high-contrast',
    fixtureId: 'home-hook',
    label: 'Home hook ember high contrast',
    specimenRoute: '/',
    selector: '#home-hook',
    conditions: Object.freeze({ themePack: 'banked-ember', highContrast: 'on' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Charcoal high-contrast ink must not land on ember’s dark ground.',
    captureValue: 'Theming check: banked-ember plus high-contrast on the home hook.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/themes/packs.css',
      'public/css/tokens/core.css',
      'public/css/effects/texture-slice.css',
    ]),
  }),
  Object.freeze({
    id: 'home-hook-glass-dark',
    fixtureId: 'home-hook',
    label: 'Home hook glass dark',
    specimenRoute: '/',
    selector: '#home-hook',
    conditions: Object.freeze({ themePack: 'glass-console', colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Glass-console dark keeps teal structure without flattening the wash.',
    captureValue: 'Theming check: glass-console dark on the home hook.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/themes/packs.css',
      'public/css/effects/texture-slice.css',
    ]),
  }),
  Object.freeze({
    id: 'home-hook-electric',
    fixtureId: 'home-hook',
    label: 'Home hook electric studio',
    specimenRoute: '/',
    selector: '#home-hook',
    conditions: Object.freeze({ themePack: 'electric-studio' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Electric studio contrast should bite without making the wash a glare plate.',
    captureValue: 'Theming check: electric-studio pack on the home hook.',
    sourceFiles: Object.freeze([
      'index.html',
      'public/css/themes/packs.css',
      'public/css/effects/texture-slice.css',
      'public/css/systems/image-theming.css',
    ]),
  }),
  Object.freeze({
    id: 'about-years-dark',
    fixtureId: 'about-years',
    label: 'About years dark',
    specimenRoute: '/about/',
    selector: '#about-years',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Paper slice behind the years path must not mute resume ink.',
    captureValue: 'Theming check: dark about-years with paper texture slice.',
    sourceFiles: Object.freeze([
      'about/index.html',
      'public/css/effects/texture-slice.css',
      'public/css/routes/surfaces/about.css',
    ]),
  }),
  Object.freeze({
    id: 'about-boonhonk-ember',
    fixtureId: 'boonhonk-theme-chips',
    label: 'About boonhonk ember',
    specimenRoute: '/about/',
    selector: '#boonhonk-register',
    conditions: Object.freeze({ themePack: 'banked-ember' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Crown still and register copy have to sit in ember without a paper-white hole.',
    captureValue: 'Theming check: banked-ember on the boonhonk register still.',
    sourceFiles: Object.freeze([
      'about/index.html',
      'public/css/themes/packs.css',
      'public/css/systems/image-theming.css',
      'public/css/effects/texture-slice.css',
    ]),
  }),
  Object.freeze({
    id: 'recipes-hero-dark',
    fixtureId: 'recipes-hook',
    label: 'Recipes hero dark',
    specimenRoute: '/recipes/',
    selector: '.recipe-hero-figure-rail',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Wash and dough stills should seat into dark recipes without bleaching captions.',
    captureValue: 'Theming check: dark recipes hero stills with texture slices.',
    sourceFiles: Object.freeze([
      'recipes/index.html',
      'public/css/effects/texture-slice.css',
      'public/css/systems/image-theming.css',
    ]),
  }),
  Object.freeze({
    id: 'rpg-boonhonk-dark',
    fixtureId: 'rpg-boonhonk',
    label: 'RPG boonhonk dark',
    specimenRoute: '/play/rpg-wednesday/',
    selector: '#rpgw-boonhonk',
    conditions: Object.freeze({ colorMode: 'dark' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Harlequin slice on the genre bible must stay behind playable copy.',
    captureValue: 'Theming check: dark RPG boonhonk frame with harlequin texture.',
    sourceFiles: Object.freeze([
      'play/rpg-wednesday/index.html',
      'public/css/effects/texture-slice.css',
    ]),
  }),
  Object.freeze({
    id: 'rpg-boonhonk-vellum',
    fixtureId: 'rpg-boonhonk',
    label: 'RPG boonhonk ritual vellum',
    specimenRoute: '/play/rpg-wednesday/',
    selector: '#rpgw-boonhonk',
    conditions: Object.freeze({ themePack: 'ritual-vellum' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Vellum warmth plus harlequin should not collapse body ink.',
    captureValue: 'Theming check: ritual-vellum on the RPG boonhonk frame.',
    sourceFiles: Object.freeze([
      'play/rpg-wednesday/index.html',
      'public/css/themes/packs.css',
      'public/css/effects/texture-slice.css',
    ]),
  }),
  Object.freeze({
    id: 'curriculum-memory-pin',
    fixtureId: 'curriculum-hook',
    label: 'Curriculum memory pin',
    specimenRoute: '/curriculum/#memory-buffers',
    selector: '#memory-buffers',
    attention: Object.freeze({ section: 'memory-buffers' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'A deep link to the taught buffer must pin that room, not the hero.',
    captureValue: 'Attention pin: fragment + region-mark=capture on memory-buffers.',
    sourceFiles: Object.freeze([
      'curriculum/index.html',
      'public/js/runtime/attention/capture-pins.js',
    ]),
  }),
  Object.freeze({
    id: 'software-frame-probe',
    fixtureId: 'software-hook',
    label: 'Software frame probe',
    specimenRoute: '/topics/software/',
    selector: '#software-surface',
    attention: Object.freeze({ probe: 'frame' }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Pinning frame should lift neighbor #> chips through the inheriting resonance report.',
    captureValue: 'Attention probe: data-spw-resonance-probe=frame spends --spw-attention-resonance on operators.',
    sourceFiles: Object.freeze([
      'topics/software/index.html',
      'public/css/tokens/core.css',
      'public/css/effects/wonder.css',
      'public/js/runtime/attention/capture-pins.js',
    ]),
  }),
  Object.freeze({
    id: 'curriculum-hero-focus',
    fixtureId: 'curriculum-hook',
    label: 'Curriculum hero focus',
    specimenRoute: '/curriculum/',
    selector: '#curriculum-hero',
    prepare: Object.freeze({
      focus: '[data-spw-operator="frame"]',
    }),
    assertAttention: 'spend',
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Focus inside the hook must lift rest ink. If opacity stays 0.86, reports are not being spent.',
    captureValue: 'Attention spend: :focus-within rest beats and --spw-attention-opacity leave the rest floor.',
    sourceFiles: Object.freeze([
      'curriculum/index.html',
      'public/css/tokens/core.css',
      'public/css/typography/typesetting.css',
    ]),
  }),
  Object.freeze({
    id: 'folio-fold-open-reduced',
    fixtureId: 'folio-fold-probe',
    label: 'Folio fold open reduced motion',
    specimenRoute: '/design/folios/#folio-fold-probe',
    selector: '#folio-fold-probe',
    conditions: Object.freeze({ reducedMotion: 'reduce' }),
    prepare: Object.freeze({
      check: Object.freeze(['#folio-fold-probe input[value="open"]']),
    }),
    layoutScenarios: Object.freeze(['pocket']),
    wonder: 'Open still has a decisive edge without relying on the slide.',
    captureValue: 'Environment check: prefers-reduced-motion on the open fold probe.',
    sourceFiles: Object.freeze([
      'design/folios/index.html',
      'public/css/routes/surfaces/design.css',
    ]),
  }),
]);

export function getViewportStillRecipe(id) {
  return VIEWPORT_STILL_RECIPES.find((recipe) => recipe.id === id)
    || VIEWPORT_STILL_CHECKS.find((recipe) => recipe.id === id)
    || null;
}
