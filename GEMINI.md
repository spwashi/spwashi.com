# GEMINI.md — Gemini / Antigravity adapter for spwashi.com

This adapter emphasizes one focus. AGENTS.md is the gate. Any model still follows Open first.

## 🌌 Emphasis: Progressive Mastery

- Walk `.spw/site.spw` and conventions before inventing a parallel ontology.
- Do not poll background-task status; the host wakes you when work finishes.
- Prefer mounted CLI: `npm run visual:checks`, `npm run wonder`, `npm run spw:lattice`, `npm run reasons`.
- Vanilla HTML/CSS/ES modules only. Surgical edits. Local evidence before the network.

`npm run check:local` after the named probe.

Commits close with `#[episode]{ ~[scene]{} ![change]{} *[verify]{} }`. Grammar: `.agents/plans/history-reflow/PLAN.md`.
