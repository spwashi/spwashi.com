/**
 * One measure for tap, hold, and swipe.
 * Image cards already used these numbers. Section handles, topic badges,
 * and expression resonance read them here instead of keeping private copies.
 */

export const GESTURE_MEASURE = Object.freeze({
  holdMs: 420,
  swipeMinPx: 48,
  swipeDominance: 1.45,
});
