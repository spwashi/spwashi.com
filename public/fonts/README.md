# Local webfonts

JetBrains Mono (OFL) and Newsreader (OFL). Self-hosted so first paint does not
wait on fonts.googleapis.com. Latin faces load first; latin-ext is a second
`@font-face` with the same family name.

Source: Google Fonts static files, 2026-08-18. See
`public/css/tokens/fonts.css`.
